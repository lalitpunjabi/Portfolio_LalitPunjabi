apiVersion: v1
kind: Secret
metadata:
  name: portfolio-secrets
  namespace: default
  labels:
    app: portfolio-ui
type: Opaque
stringData:
  # ⚠️ SECURITY WARNING: In a true production environment, never commit raw secrets.
  # Use tools like HashiCorp Vault, AWS Secrets Manager, or Bitnami SealedSecrets.
  # This file serves as an architectural placeholder for CI/CD secret injection.
  PLACEHOLDER_KEY: "to-be-replaced-by-github-actions"
